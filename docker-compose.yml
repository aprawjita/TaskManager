services:
  db:
    image: postgres:15
    container_name: task_manager_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: taskmanagerdb
    ports:
      - "5433:5432"
     